Drop client image assets here:
- logo.png        -> official DermaMedics gold logo (transparent PNG)
- hero-model.png  -> hero model cutout (transparent PNG, portrait ~4:5)

The hero references /photos/hero-model.png and shows an elegant magenta/gold
gradient fallback until the file is added (no broken image).
